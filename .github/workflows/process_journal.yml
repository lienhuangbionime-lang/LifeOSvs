name: Process Journal Entry

on:
  repository_dispatch:
    types: [new_journal_entry]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout content
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas pyarrow python-frontmatter requests

    # 1. 思考與寫入 (Inbox)
    - name: Run Neural Classifier (Gemini)
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        JOURNAL_TEXT: ${{ github.event.client_payload.text }}
      run: python src/actions/process_inbox.py

    # 2. 執行閉環 (Tasks) - NEW
    - name: Sync Tasks to Google
      env:
        ZAPIER_TASK_WEBHOOK: ${{ secrets.ZAPIER_TASK_WEBHOOK }}
      run: python src/actions/sync_tasks.py

    # 3. 專案分流 (Classification)
    - name: Run Dual-Track Splitter
      run: python src/actions/classify_inbox.py

    # 4. 壓縮與歸檔 (Compaction)
    - name: Run compaction script
      run: python src/actions/compact_inbox.py

    # 5. 生成戰情報告 (Reporting) - NEW
    - name: Generate Project Report
      run: python src/actions/generate_report.py

    # 6. 一次性提交所有變更
    - name: Commit and push changes
      uses: stefanzweifel/git-auto-commit-action@v5
      with:
        commit_message: "🤖 LifeOS: Processed, Synced & Reported"
        file_pattern: "data/*"
